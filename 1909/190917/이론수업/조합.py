# nCr 할때 r==0 이거나 r==n인 시점에서 종료
# an = 전체 모집합
# tr = 선택된애. 조합 임시 저장 배열
# def comb(n, r):
# #     if r == 0: print()
# #     elif n < r: return
# #     else:
# #         tr[r-1] <- an[n-1]
# #         comb(c-1, r-1)
# #         comb(n-1, r)